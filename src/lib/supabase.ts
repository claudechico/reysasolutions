// Supabase integration removed.
// This file previously created a Supabase client. The project no longer uses Supabase; API wrapper (`src/lib/api.ts`) is used instead.

// Export minimal stubs so any remaining imports don't break the build.
export const supabase: any = null;

export type Profile = any;
export type Property = any;
export type PropertyMedia = any;
export type Booking = any;
export type Message = any;
export type Inquiry = any;
export type Favorite = any;
